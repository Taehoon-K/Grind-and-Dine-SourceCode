using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*public class CheckPlacement : MonoBehaviour
{
    BuildManager buildManager;

    //[SerializeField] private Material materials;
    void Start()
    {
        buildManager = GameObject.Find("Player").GetComponent<BuildManager>();
        //this.gameObject.GetComponent<MeshRenderer>().material = materials;
        //this.gameObject.transform.GetChild(0).GetComponent<MeshRenderer>().material = materials;
    }


    private void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("ItemActive"))
        {
            buildManager.canPlace = false;
        }
    }
    private void OnTriggerExit(Collider other)
    {
        if (other.gameObject.CompareTag("ItemActive"))
        {
            buildManager.canPlace = true;
        }
    }
}*/
